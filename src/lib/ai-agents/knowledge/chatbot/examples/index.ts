// GOLDEN EXAMPLES — образцовые воронки.
// Модель копирует стиль из этих примеров вместо того чтобы изобретать с нуля.
// Это самый мощный рычаг качества.
//
// Как добавлять новый пример:
// 1. Создай файл `01-название.ts` рядом
// 2. Экспортируй const как Example (см. type ниже)
// 3. Добавь импорт в массив EXAMPLES ниже
//
// Чем больше разных ниш покрыто — тем лучше модель работает с новыми.
// Оптимум: 5-8 примеров покрывающих основные типы бизнеса клиентов.

export type Example = {
  /** Короткое название: "Онлайн-курс по Python", "Психолог", "Коучинг" */
  niche: string
  /** Описание воронки в 1-2 строках */
  goal: string
  /** Сам текст воронки в формате который агент должен копировать.
   *  Пиши как markdown, каждое сообщение/дожим озаглавлено, с комментариями почему текст именно такой. */
  funnel: string
}

import { FORMAT_SAMPLE } from './00-format-sample'

export const EXAMPLES: Example[] = [
  FORMAT_SAMPLE,
  // сюда добавляй следующие примеры — 01-*, 02-*, ...
]
